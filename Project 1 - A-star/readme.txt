Stephen Albrecht
Ryan Gray

Note: it is assumed that locations.txt and connections.txt are in the same directory as the .exe file. 
To create a "Network.exe", use the following command with a g++ compiler installed:

g++ -o Network Network.cpp
